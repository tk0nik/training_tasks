#Write a program that takes a list of integers as input and displays values ​​that are repeated more than once in it.

nums = input().split()
result = []
for i in nums:
    if nums.count(i)>1:
        result.append(i)
print(*set(result))
