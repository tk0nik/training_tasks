#===== УСЛОВИЕ =====#

#Дан массив АА из пп целых чисел. Выведите YES, если массив является красивой горой, иначе - NO.
#Массив называется красивой горой, если с начала массива элементы строго возрастают, а далее с какого-то момента строго убывают.

#Входные данные
#В первой строке задается одно натуральное число п \ ge3п≥3, не превосходящее 100100.
#Во второй строке вводятся пп целых чисел, каждое из которых по модулю не превосходит 100100. 

#Выходные данные
#Выведите YES, если массив является красивой горой, иначе - NO.

#===== РЕШЕНИЕ =====#
n = int(input())
l = [int(i) for i in input().split()]
imax = l.index(max(l))
if l.count(max(l))==1 and imax != 0 and imax != n-1:
    if l[:imax+1] == sorted(list(set(l[:imax+1]))) and l[imax+1:] == sorted(list(set(l[imax+1:])), reverse=True):
        print('YES')
    else:
        print('NO')
else: 
    print('NO')
