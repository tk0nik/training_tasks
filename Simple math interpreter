# ========== task ========= #
'''Write a simple math interpreter.

The input is a string with an expression consisting of two numbers united by a binary operator: a {operator} b, where instead  {operator} the following words can be used: plus, minus, multiply, divide for, respectively, addition, subtraction, multiplication and integer division.'''

# ==== data ==== #
string = input().split()

# ==== functions ==== #
def calculate(x, y, operator):
    if operator == 'plus':
        return(x+y)
    elif operator == 'minus':
        return(x-y)
    elif operator == 'multiply':
        return(x*y)
    elif operator == 'divide':
        return(x//y)
    return('Error: incorrect operator')

print(calculate(float(string[0]),  float(string[2]), string[1]))
 
